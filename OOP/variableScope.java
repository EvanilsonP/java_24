package OOP;

public class variableScope {
    
    public static void main(String[] args) {
        // LOCAL VARIABLE = declared inside a method / visible only to tha method
        // GLOBAL VARIABLE = Declared outisde a method, but within a class / visible to all parts of a class
        
        @SuppressWarnings("unused")
        DiceRoller diceRoller = new DiceRoller();
    }

}
